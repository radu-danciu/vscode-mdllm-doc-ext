## scripts/publish-release.cjs

### `runNpx(args, cwd, env)`

Brief: Describes the repo-local function `runNpx` in `scripts/publish-release.cjs`.

Details:
This self-hosted entry keeps the workspace fully dogfooded for hover, definition, and markdown lookup flows.

Params:

- `args`: Input accepted by `runNpx`.
- `cwd`: Input accepted by `runNpx`.
- `env`: Input accepted by `runNpx`.

---

### `publishRelease(rootDir)`

Brief: Describes the repo-local function `publishRelease` in `scripts/publish-release.cjs`.

Details:
This self-hosted entry keeps the workspace fully dogfooded for hover, definition, and markdown lookup flows.

Params:

- `rootDir`: Input accepted by `publishRelease`.

---
